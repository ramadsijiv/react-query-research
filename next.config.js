/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,  // ! fetch 2 times (useEffect) even with [] deps in dev mode https://byby.dev/useeffect-run-twice
}

module.exports = nextConfig
